# VasDolly 渠道打包

这个上下文描述 Android APK 渠道打包领域的共同语言，帮助工程决策和交付保持一致。

## 渠道分发

**渠道**：标识 APK 将要发布到的分发渠道的字符串。
_避免_：平台、来源、变体

**基础 APK**：生成渠道包前作为输入的 APK 文件。
_避免_：母包、原包

**渠道包**：携带一个指定渠道信息、可独立发布的 APK 文件。
_避免_：分包、渠道变体

**渠道信息**：渠道包中用于识别其渠道的元数据。
_避免_：渠道号、渠道标签

## APK 签名

**签名方案**：APK 使用的 Android 签名版本；本项目关注 V1、V2 和 V3。
_避免_：签名模式（描述检测结果时可使用“模式”，领域概念统一称“方案”）

**V1 签名**：基于 JAR/META-INF 的 APK 签名方案，兼容较早的 Android 平台。

**V2/V3 签名**：基于 APK Signing Block 的 APK 签名方案，覆盖现代 Android 平台；V3 还携带签名者的 SDK 范围。

**Signing Block**：位于 APK 内容与 ZIP 中央目录之间、承载 V2/V3 签名及其他 ID-value 元数据的结构。
_避免_：签名段、签名块（面向用户说明时可作为释义）
